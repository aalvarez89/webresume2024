import css from './Styles/Landing.module.scss'

const Landing = props => {

    // const { title, description, innerContent, even } = props;

    return (
        <div className={css.landing}>
            
        </div>
    )
}

export default Landing;